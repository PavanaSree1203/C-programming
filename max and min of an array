//min and max of an array
#include<stdio.h>
main()
{
	int i,n,a[10],min,max;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	min=a[0];
	max=a[0];
	for(i=0;i<n;i++)
	{
		if(min>a[i])
		min=a[i];
		else if(max<a[i])
		max=a[i];
	}
	printf("%d %d",max,min);
}
